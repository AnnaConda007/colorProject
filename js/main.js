// вызов функций
window.addEventListener("DOMContentLoaded", () => {
  "use strict";
  modal(".triger", ".form-substrate", ".closeBtn");
});

// modals
const modal = (trigerSelecrtor, modalSelector, closeSelector) => {
  const openModalBtn = document.querySelectorAll(trigerSelecrtor);
  const modal = document.querySelectorAll(form - substrate);
  const closeModal = document.querySelectorAll(closeBtn);

  // не забудь, что используется массив querySelectorAll
};
